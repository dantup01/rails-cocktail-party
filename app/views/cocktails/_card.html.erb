<div class = "container">
  <div class="wrapper">
    <div class="cards">
      <% @cocktails.each do |cocktail| %>
        <figure class="card">
            <%= cl_image_tag cocktail.photo.key, alt: "alttext" %>
            <figcaption><%= link_to cocktail.name, cocktail_path(cocktail) %></figcaption>
        </figure>
      <% end %>
    </div>
  </div>
</div>
